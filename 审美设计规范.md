# SCMC 学生新媒体中心 · 审美与设计规范（可复用版）

> 本文档整理自历次项目的完整需求与反馈，可作为**任何新项目 / 改版 / 新成员接手**时的直接输入。
> 使用方式：把本文档整体交给执行方，并补充当次项目的具体内容清单即可。

---

## 0. 一句话定位

**品牌官网 + 数字艺术展览 + 校园创意市集** 三者的交集：
把校园新媒体社团的视觉语言，做成一间"线上影像展厅"，而不是商城。

四条优先级（永远不变）：

1. **信息准确**
2. **页面稳定**
3. **交互流畅**
4. **视觉高级**

---

## 1. 设计气质

### 关键词

校园 / 青春 / 新媒体 / 影像 / 创作 / 高级 / 克制 / 动态 / 展览感

### 要做

- 视觉高级、精致、有设计感；动画丰富但**不杂乱**
- 产品图片展示突出；页面切换与滚动流畅
- 有明确的"学生新媒体中心"视觉识别
- 像一次完整的"线上视觉展览"，有编辑感

### 绝不做

- 淘宝 / 拼多多 / 传统电商风、廉价模板感
- 花哨渐变、满屏发光、过度赛博朋克、低质量科技蓝
- "限量""爆款""超值""必买""抢购倒计时"等电商营销文案
- 儿童商城、卡通玩具感
- 大量商品格子堆砌

---

## 2. 色彩系统

| 角色 | 色值 | 用途 |
|---|---|---|
| 墨黑 | `#0b0b0e` | 页面底色 |
| 深灰阶 | `#101116` / `#14151b` / `#16171d` | 卡片、悬浮层、图片衬底 |
| 纸白 | `#f2efe6` | 主文字、强调块 |
| 灰字 | `#8a8a93` / `#5b5c66` | 次要文字、技术标签 |
| REC 红 | `#ff4d2e` | **唯一强调色**（REC 点、价格、焦点、警示） |
| 描边 | `rgba(242,239,230,0.14)` | 边框、分隔线 |

补充规则：

- 允许**少量低饱和蓝 / 紫 / 青**作为新媒体点缀色，克制使用
- 校园格纹元素可以**适量**加入，不整页铺满
- 全站强调色只保留一个 REC 红，其余全部靠黑白灰层级

---

## 3. 字体与排版

- 中文正文：系统无衬线栈
  `-apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB", "HarmonyOS Sans SC", "MiSans", "Microsoft YaHei", sans-serif`
- 大标题：可选用衬线宋体系（Songti / Noto Serif CJK SC）营造展览感，也可用粗无衬线；标题可稍微加粗，正文保持克制
- 英文 / 数字 / 技术标签：等宽字体（SF Mono / JetBrains Mono / Menlo / Consolas），用于时间码、编号、规格
- 字号用 `clamp()` 流式，覆盖 390 / 768 / 1280 / 1440 / 1920
- **换行规则**：标题按语义断行（如"让校园里的创意，\n走得更远"），不要压缩成一行；保持视觉呼吸
- **对齐规则**：同组内容底部基线一致（文字栏与卡片/模块底部对齐，不允许上下错位）
- 大号描边英文（stroke text）必须完整显示，不截断；超宽时允许横向滚动

---

## 4. 新媒体视觉语言（设计语言，不是图标堆砌）

可用元素清单（作为氛围与细节出现，**禁止满屏贴图标**）：

时间码（HH:MM:SS:FF）· REC 红点 · Frame 帧号 · 取景框四角 · 对焦框 · 剪辑时间线轨道（V1/V2/A1/A2）· 胶片齿孔传送带 · 扫描线 · 胶片颗粒（grain）· 素材编号（SM-xxx / PC-xx）· 场记板条纹 · 播放进度条 · 波形 · 裁切框 · 时间戳

原则：

- 元素融入版式与动效（页眉时间码、卡片编号、加载进度条），而不是"贴一个相机图标"
- 目标是"看起来像新媒体"，不是"告诉别人这是新媒体"

---

## 5. 布局与组件规范

- **商品卡片统一对齐系统**：同组卡片尺寸一致（如 4:5），图片区、名称、介绍、价格位置完全对齐；不允许多种卡片高度混排导致错乱
- **图片一律 `object-fit: contain`**，商品主体绝不裁切、绝不拉伸、绝不改变比例；需要裁切时显式说明，绝不粗暴 cover
- 图片容器提前定义 `aspect-ratio`：加载前后不塌陷、不跳动；配 skeleton/占位底色
- 桌面 3–4 列、平板 2–3 列、手机 1–2 列；手机端重新规划（字体、排列、菜单、点击区域），不是 PC 缩小
- **导航**：顶部与底部**完全一致**（顺序、名称、锚点）；移动端全屏菜单同样一致，支持 ESC 关闭
- 详情页统一结构：返回商品列表 → 视觉区 → 名称 → 价格 → 一句话描述 → 规格/套餐内容 → 注意事项 → 相关图片 → 如何购买 → 相关推荐
- 页面之间保持连续：同一背景、圆角、字体、标题层级、编号、动效速度、颜色、icon 风格、间距体系、返回按钮
- 二维码区 = 文字说明 + 二维码的统一视觉模块，与下方内容底部对齐，不漂浮
- 套餐商品带轻量 `SET / 组合` 角标（不用"爆款/超值"）

---

## 6. 动效规范

### 允许

`opacity / translate / scale / blur / clip-path / mask / stagger`
缓动以 expo-out（`cubic-bezier(0.16,1,0.3,1)`）为主；单次过渡 **450–750ms**，片头 ≤1.5s 且可跳过

### 禁止

- 电商式弹跳、夸张旋转、花哨粒子、强制 3D
- 复杂 WebGL、商品图 3D 飞入、强制 shared-element transition
- 大量 absolute 卡片动画、无意义旋转
- 页面滚动锁死、鼠标跟随导致图片偏移
- 任何**导致错位 / 跳动 / 闪烁**的动画——一律取消，**稳定 > 炫技**
- 动画结束后清理 transform，避免影响后续定位

### 性能红线

- 动画只用 transform/opacity；backdrop-blur 仅交互期间启用
- 不做"每张商品图一个 ScrollTrigger"、不跑几十个常驻 rAF
- resize/分类切换后的重算必须防抖（ScrollTrigger.refresh 在布局稳定后执行）
- 所有滚动监听/观察器正确 cleanup；交付前 0 console error
- `prefers-reduced-motion` 必须降级；触屏关闭自定义光标与 hover 依赖

---

## 7. 交互模式库（已验证的范式，可直接复用）

| 场景 | 范式 |
|---|---|
| 明信片/卡片堆叠 | 默认自然堆叠（位移+角度差+层级+阴影）→ hover 轻微扇形 → 悬停单张聚焦（**选中卡不动**，其余让开）→ 点击取出放大 → 后方高斯模糊+压暗 → 点击另一张立即切换 → 点空白/✕/ESC 放回 → 重置按钮默认隐藏、点击过才出现 |
| 信纸/纸张 | hover = 小角度预览翻开（不穿模、不抖动、移开自然恢复）；click = 打开详情查看；**预览与查看不混用** |
| 滚轮控件（万年历） | 滚轮在控件区域内拦截（preventDefault），连续可翻、可前可后、不锁死；不带走页面滚动；手机横向滑动切换 + 纵向滚动不受阻 |
| 夜灯 | 点击明暗切换 + 环境变暗 + 光晕；标明"网页演示，非产品功能" |
| 分类切换 | 旧卡片 opacity 1→0 → 更新数据 → 高度重算 → 新卡片 0→1；ScrollTrigger.refresh 在布局稳定后 |
| 详情返回 | 恢复原分类、原滚动位置、原卡片状态；不跳回顶部、不重播首页动画 |
| 展开状态 | 由点击控制，**不依赖 hover / mouseleave**；支持连续切换 |

---

## 8. 内容真实性原则（最高优先级）

- 只写已确认的事实；材质、工艺、设计来源、活动背景、包装、功能一律不虚构
- 未确认信息用固定表述：价格 →"价格待更新/价格请咨询"、版本 →"当前展示版本尚未最终确定，仅供参考"、整体 →"一切以实物为准"
- 图片处理流程固定：保存原图 → 另存处理版 → 抠图/清背景 → 保持商品真实外形（不改产品上的文字图案结构）→ 构图/光影/透视 → 按卡片比例裁切 → WebP 压缩 → 替换 → 同步更新价格与介绍
- 数据与图片按**文件名严格对应**，价格以确认信息为准，全站只允许一个数据源
- 免责声明必备三项：版本未定提示、图片仅供参考、最终解释权归社团所有
- 不出现虚假联系方式、不自行编造邮箱/地点

---

## 9. 文案语气

- 克制、真实、有感情（参照凉山公益文案的口吻：不夸张、不替善意定义结果）
- 不写电商促销话术；公益/理念类文案可以有小标题与断句节奏
- 字数少而准，不留无意义长段落

---

## 10. 响应式

覆盖：1920×1080 / 1440×900 / 1366×768 / iPad 横竖屏 / iPhone 与主流安卓（390 基准）。
每档单独检查：横向溢出为 0、菜单可达、点击区域 ≥44px、动画降级、图片不变形。

---

## 11. 工程结构要求

- 组件化：layout / product / sections / ui / animation 分层
- **商品数据单一来源**（一个 products 数据文件），页面零硬编码；新增商品 = 加一条数据 + 一个图片目录
- 图片路径统一走 BASE_URL 感知的助手函数（兼容任意子路径部署）
- 关键 key 使用稳定唯一 id，禁止用数组下标
- 交付自检清单：价格全对 / 无错位 / 无长空白 / 无溢出 / 无 console error / reduced-motion 可用 / 移动端与 PC 同等精度

---

## 12. 部署基线

- 纯静态站点：Cloudflare Pages（主）与 GitHub Pages（备）双平台可同时部署
- 构建产物相对路径，任意根路径可用；验证文件可直接放 `public/` 根目录
- 一键脚本（.command）交付使用者，更新=重跑脚本

---

*最后修订：2026-08 · 适用项目：SCMC 义卖展示网站及后续社团数字项目*
